apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-hooks.fullname" . }}
  labels:
    {{- include "simple-hooks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-hooks.selectorLabels" . | nindent 4 }}
